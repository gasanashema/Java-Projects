# Inheritance
- It is a way of creating a class from an existing class
- Among the classes one shuld be a parent class and the other should be a child class
- The parent class is called the superclass and the child class is called the subclass
- *To establish inheritance we use extends keyword* 